pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "arrow_fails_export_release"
include(":androidApp")
include(":shared")